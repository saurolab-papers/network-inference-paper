// Created by libAntimony v2.13.2
model *stafford1()

  // Compartments and Species:
  species T, Ti, V;

  // Reactions:
  v1:  -> T; lambda;
  v2: T -> ; d*T;
  v3: T -> Ti; k*T*V;
  v4: Ti -> ; delta*Ti;
  v5:  -> V; pie*Ti;
  v6: V -> ; c*V;

  // Species initializations:
  T = 10000;
  Ti = 0;
  V = 0.001;

  // Variable initializations:
  c = 3;
  d = 0.013;
  delta = 0.4;
  k = 4.6e-07;
  lambda = 130;
  pie = 980;

  // Other declarations:
  var c, d, delta, k, lambda, pie;
end
